import { Routes } from '@angular/router';
import { GroceriesComponent } from './groceries.component';

export const GROCERIES_ROUTES: Routes = [
    {
        path: '',
        component: GroceriesComponent,
    }
];
